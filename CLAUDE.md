# Activation Model — Session Orientation

## Read first, every session
1. `activation-model.md` — the spec. It is law. Code conforms to it; it changes only by Jocke's explicit acceptance. On any conflict between files, the spec wins.
2. `build-brief.md` — process, build order, measurements, hard rules.

## Current state
- Phase 1 (message board) built, measured, and **accepted by Jocke as-is** (2026-07-08); spec unchanged. Grammar + runtime in `am/` (byte-walking instruction grammar, single router, client), workload as instructions, end-to-end demo with restart survival (`demo.py`), measurements (`report.py`, `REPORT.md`).
- Phase 2 (computation pressure: find/count/newest queries over phase-1 data) built and measured same day. Library grew 2 → 5 structures (`select`, `tally`, `last` — all pure, conditions as values); grammar unchanged at 4 nodes. No falsification event; growth curve in `REPORT.md`.
- Phase 3 (the layer, per `PHASE3-LAYER.md` — read it before touching phase-3 code) complete same day: real FUSE mount (`am/layer.py`, `am/fuse.py`), all four acceptance tests pass including unmodified git; offline condensation pass (`am/condense.py`) admitted 5 content templates; growth curve 2 → 5 → 10 (threshold hit = reported finding). `PHASE3-STATE.md` is that phase's session-state file.
- Phase 4 (computation pressure + shape condensation, 2026-07-09) complete: universal tier grew 5 → 10 (`sum`, `sort`, `div`, `sub`, `pick`), while EIGHT computations were composed with no new structures (max/min/avg/count-over/top-N/join — LIBRARY.md table) — first sub-linear phase. `am/shapemine.py` promotes recurring composition shapes as resident structures with value slots (arrival = recognition); 2 shapes admitted, idempotent, render-verified. `phase4.py` reproduces.
- Phase 5 (the wire, 2026-07-09) complete: two routers, two worlds; replication = emits carrying opaque unsent instructions (`am/sync.py`, discovery by namespace walk — no manifest). Receiver refused condensed chains with "unknown structure id N", 84 shipped lib bytes + router reload healed it, all 26 queries byte-identical across nodes; 163-byte incremental delta. JSON smaller at toy scale (recorded honestly, FRICTION #27). New open questions: DECISIONS §2.2 (lazy structure residency — recommendation written, NOT implemented), §2.3 (remote refs — undesigned). `phase5.py` reproduces.
- Phase 6 (compaction + render cache, 2026-07-09) complete: `am/compact.py` deletes unreachable chains by head-reachability (268.9 MB → 5.7 MB, render-verified, no GC machinery); layer render cache (demander-side memory, DECISIONS 1.15, `--raw` disables) took reads 30.4 → 2.84 firings/call — the residual is the uncacheable head demand, the model's identity-over-time cost floor (FRICTION #29). `phase3_tests.py` pins `--raw` so v1 baselines stand. `phase6.py` reproduces.
- Phase 7 (multi-writer, 2026-07-09) complete: one writer per head + ownership-scoped sync (`prefixes`); merge = `sort∘concat` over demanded chains, fired at read time — byte-identical convergence on both nodes, zero coordination, zero new structures. Naive bidirectional sync reopened the §2.1 window at the copy layer; cured by ownership, logged FRICTION #31, DECISIONS 1.16. `phase7.py` reproduces.
- Phase 8 (open-key grouping, 2026-07-09) complete: friction #10 resolved by write-side projection (authors chain, fourth emit in the same post firing) — key set = `uniq∘sort∘demand`, grouping composes as phase 2; parser stays refused; universal tier 10 → 11 (`uniq`). FRICTION #34-#35. `phase8.py` reproduces.
- Phase 9 (scale, 2026-07-09) complete: 637 files / 11 MB of real stdlib source through ingest → condense → compact → verify → replicate. Fidelity held; **universal tier 11 → 11 at 25× volume**; store at ext4 parity. Honest correction: gzip beats the wire 4× on unique content — universality-compression is dedup-shaped, not entropy-shaped (FRICTION #36); edit deltas re-ship whole files (#37). `phase9.py` reproduces.
- Phase 10 (recurrence corpus, 2026-07-09) complete: 8 snapshot generations, 30.4 MB logical / 5.3 MB unique. Condensed+compacted store **4.60 MB beats tar.gz 7.63 MB (1.7×) and the whole-file-dedup floor 5.34 MB**; wire = 4.71 MB. Compression claim now honestly scoped both ways (unique → gzip wins; recurring → model wins, and stays per-file demandable). Zero universal additions, zero new machinery. FRICTION #39-#40. `phase10.py` reproduces.
- Phase 11 (native fire loop, 2026-07-09) complete: `native/amfire.c` (~300 lines, read path only — writes/queries stay in Python) fires Python-written stores byte-identically (checksum-proved). Warm firing = **0.86× of `cat`** while reading 6.6× fewer disk bytes — §0's kill condition did not trigger; cold = 0.42× (IOPS-bound reference chasing, FRICTION #41 — packed layout is an open engineering question). Python folklore corrected: direct render 85 MB/s, FUSE was the old bottleneck (#42). `phase11.py` reproduces.
- Phase 12 (the tool, 2026-07-09) complete: `am.tool` — snap/snaps/ls/cat/restore/verify/stats/serve/push/mount, a ~250-line world-side CLI composing chartered machinery only. 3 generations stored at 0.38× logical; full push 2.55 MB, **delta push 0.16 MB** (workflow answers #37: snap → condense → compact → push); snapshots browsable via mount; replica needs no import. Dogfood findings FRICTION #44 (≥3 threshold warms up late — 2-file threshold awaits ruling; deltas chunk-grained). `phase12.py` reproduces.
- Phase 13 (THE metric, 2026-07-09) complete, tightened after Jocke's python-vs-c challenge to a THREE-way: same job, identical answers — crossings per payload byte **14.15 (conv log+scan) → 2.68 (conv indexed, same projection) → 1.79 (model)**. Headline 7.9× vs naive honestly decomposes to **1.5× vs the best conventional design** — the pure one-representation dividend (encode-at-birth + decode-at-ingest are crossings the model structurally cannot pay). Wall rows labeled not-comparable (Python vs C; phase 11 = native evidence). FRICTION #46. `phase13.py` reproduces.
- Phase 14 (the real build, 2026-07-09) complete: `native/amd.c` (~550 lines) = full native router (grammar, all 11 structures, emit, templates, wire protocol). Gates: Python-vs-amd ingest **byte-identical world trees**, 4-pipeline answer identity, refusal parity. True clocks: interpreter gap closed (queries 1.695→0.113 s); residual is the one-instruction-one-file LAYOUT (IOPS: 20k creates at ingest, 2k opens/query → ~7× behind best conventional on record-granular work). Packed-container references escalated to DECISIONS §2.4 (recommendation written, NOT implemented). FRICTION #47. `phase14.py` reproduces.
- Phase 15 (the compute half, 2026-07-09) complete: a sequential approval program (208 decisions, each dependent on all previous) — every decision identical to a plain-Python reference, restart-proof mid-run. **Branching composed with ZERO new structures** (condition = `tally∘pick` value; branch = `select` over data alternatives). Boundary drawn honestly (FRICTION #49): the model decides and renders; the world pumps (effects, sequencing, loops) — a decision-and-rendering engine, not a computer; A4 arguably forbids more. `phase15.py` reproduces.
- Phase 16 (the memory path, 2026-07-09) complete, chartered by Jocke's relayed critique that nothing had measured §0's actual mechanism: compiled C, same kernel, same logical stream in raw vs instruction-grammar form (L1-resident template table), cachegrind misses, recurrence sweep. **Traffic domain: claim holds — 0.29× bytes, 0.15× LL misses at p=99; falsification case (+13% at p=0) behaved as predicted. Time domain: no conversion on this VM** — linear scans are prefetch-hidden; the win needs bandwidth/energy-scarce conditions (real hardware + PMU = the open question). FRICTION #51. `phase16.py`/`native/membench.c` reproduce. `hwbench.py` = the portable rig (perf/simpleperf/RAPL/battery-drain, thermal-safe ABBA); first real-hardware datapoint (Jocke's Pixel 10 Pro XL, 2026-07-11, in REPORT.md): contended penalty 1.31× → 1.04× as p 0→99 — converging under bandwidth pressure, no crossover; energy verdict pending.
- Phase 17 (amx, 2026-07-10) complete, chartered by Jocke's "you built none of this to actually be fast or work like this" — recorded as the §2.4 ruling (references may name into a container; DECISIONS 1.18): `native/amx.c` = the §8 engine designed for the machine — one mmap'd append-only pack, references as offset-names (`@1f4a`, demand = pointer arithmetic, no syscall), zero-copy slice values, per-request bump arena, emit `@` returns the store-assigned name. Gated (4-pipeline answer identity, render identity, refusal parity, kill‑9 restart, tail derivable by grammar walk). **Queries 7–9× faster than amd (0.010 s vs ~0.08 s; gap to best conventional ~19× → ~2.5×, residual = rendering not IOPS); store 20,010 files → 2; ingest still edges amd (2.5–2.6 s vs 2.7–3.4 s) despite round-trip-per-record.** Honest price: store-assigned names couple chain writes to the round trip — the ~10× ingest gap to the conventional stream is latency floor, not bytes — FRICTION #52-#53. Universal tier 11 → 11. `phase17.py` reproduces.
- Phase 18 (the recognition path, 2026-07-11) chartered by Jocke's "you never use cached shapes?": (a) shape-compiled firing built into `membench` (`amc` — plan per shape, masked-compare recognition), gated identical, **rejected by the data at 3-field record size** (generic decode of tiny predictable records is near-free) — FRICTION #54; (b) the hunt found the real killer: **LEB128 width entropy** — hot sids straddling the varint boundary made reference length a per-record coin-flip branch; uniform-width allocation (DECISIONS 1.19, zero grammar change) sped the walk 1.9× and produced the **first time-domain wins: walk-bound job 0.46× single-core, 0.44× all-cores** (compute-heavy job 1.12× — representation matters where movement is the work) — FRICTION #55. Phase 16 baselines stand (default base = worst case). `hwbench.py` reports it all. Pixel 10 Pro XL rerun (2026-07-11): **all-cores 0.85× — first real-hardware time win** (was 1.04× straddled); single-core walk-bound 1.45× vs VM's 0.46× — conversion boundary mapped: model wins time where bandwidth is contended, loses where a lone fast core has bandwidth to spare. Energy verdict pending (Termux:API).
- External adversarial review (2026-07-11, `CRITICAL-REVIEW.md` on branch `claude/critical-build-review-42kzhx`) accepted and answered: dispositions in `RESPONSE-TO-REVIEW.md`, corrections in REPORT.md's top ERRATA section, FRICTION #56. Confirmed + fixed: phase-16 table contradicted its committed JSON (now recomputed from evidence; honest p=99 wall = 1.2–1.5×); phase-10 claim was gzip-only (xz 6× smaller, ext4 blocks lose to tar.gz — retracted as ranking claim, scoped to mechanism + per-file demandability); phase-11 baseline relabeled (not cat); crossings metric published under both accountings (strict accounting flips the sign; amx remeasure = open); three hostile-input crash paths fixed (`robustness_tests.py` pins NUL ref, 60k-deep nesting, corrupt head, garbage, traversal); emit-disk now write-then-`os.replace` (process-crash consistency honest; power-loss/fsync explicitly NOT claimed); structure-count headlines restated two-tier (universal 11 flat + template tier grows with data). Claim discipline going forward: ranges not single runs, both accountings where definitional, blocks not apparent bytes.
- `LIBRARY.md` is the structure log (primary experimental output). `FRICTION.md` holds where the model fought back (#8 is phase 2's central finding; #15 is phase 3's). `DECISIONS.md` §2.1 (atomic posting) is **ruled closed-as-open by Jocke**: the lost-update window is an accepted trade of literal references — do not fix or reopen.

## Rules that apply before you've read anything else
- Never let a second representation of anything come into existence (spec §0).
- Nothing that fires values ever enters the library — no eval, no apply, no universal interpreter. A workload that seems to need one is a §6 falsification event: reported, not implemented.
- Structural decisions where the spec is silent: present options to Jocke and wait. If asking is blocked: STOP, write options + recommendation to DECISIONS.md, end the session. Never implement the recommendation, even reversibly.
- When the model fights convention, the model wins until it demonstrably can't — and "demonstrably can't" is a finding for the friction log, not a workaround.
